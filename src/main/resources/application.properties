spring.application.name=SaccoSystem

# Database (Secure)
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Security (Secure)
app.jwt-secret=${JWT_SECRET}
app.jwt-expiration=${JWT_EXPIRATION}

# Security (AUTH)
app.default-admin.email=${DEFAULT_ADMIN_EMAIL}
app.default-admin.password=${DEFAULT_ADMIN_PASSWORD}

server.port=8080

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=jaytechwavesolutions@gmail.com
spring.mail.password=fgcg snqc gokd ygni
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=3000
spring.mail.properties.mail.smtp.writetimeout=5000
app.email.from=System admin @sacco-management-system <jaytechwavesolutions@gmail.com>
# File Upload Limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB